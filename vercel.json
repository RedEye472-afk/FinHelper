{
  "buildCommand": "cd frontend && npm install && npm run build && cd ../backend && go build -o ../api/bootstrap ./cmd/vercel/",
  "outputDirectory": "frontend/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
